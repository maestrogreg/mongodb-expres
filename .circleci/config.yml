version: 2.1

jobs: 
  builder:
    docker:
    - image: circleci/node

    steps:
    - checkout
    - run: echo "hello world"
    - run: echo "hello world2"

workflows:
  builder:
    jobs:
    - builder