version: 2.1

workflows:
  builder:
    jobs:
    - starter
    
jobs: 
  starter:
    docker:
    - image: circleci/node

    steps:
    - checkout
    - run: echo "hello world"
    - run: echo "hello world2"
