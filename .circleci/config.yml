version: 2.1

workflows:
  builder:
    jobs:
    - starter
    - ender: 
        requires:
            - starter
executors:
  docker_container:
    - docker:
      - image: circleci/node

jobs: 
  starter:
    docker: docker_container

    steps:
    - checkout
    - run: echo "hello world ${env}"
    - run: echo "hello world2 ${AUTH}"
    environment:
        AUTH : USER

  ender:
    docker:
    - image: circleci/node

    steps:
    - checkout
    - run: whoami
    
